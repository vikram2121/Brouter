[phases.build]
cmds = ["npm ci", "npm run build", "cd client && npm ci && npm run build"]

[start]
cmd = "node -e \"const s=process.env.SERVICE; require('./dist/'+(s==='worker'?'worker':s==='oracle'?'oracle':'index')+'.js')\""
